# ══════════════════════════════════════════════════════
#  Multimodal Video Intelligence — requirements.txt
#  All packages are open-source (Apache / MIT / BSD).
#  LLM inference via Ollama (local) or Claude (cloud).
# ══════════════════════════════════════════════════════

# ── Core ────────────────────────────────────────────
torch>=2.11.0               # CUDA 12.8 wheels: pip install torch --index-url https://download.pytorch.org/whl/cu128
torchvision>=0.26.0
torchaudio>=2.11.0
numpy>=2.0.0
scipy>=1.11.0               # Speaker clustering (cosine similarity)
omegaconf>=2.3.0
pyyaml>=6.0.2
httpx>=0.28.0               # Async HTTP — LLM client, Ollama API

# ── Computer Vision ─────────────────────────────────
ultralytics>=8.3.0          # YOLOv8/v12 (Apache-2.0)
opencv-python>=4.10.0       # BSD
scikit-image>=0.24.0        # BSD  — SSIM adaptive sampler
# Florence-2 uses transformers (already listed under ASR) — no extra install needed
# timm is required by Florence-2's DaViT vision encoder

# ── Geospatial (orthorectification) ─────────────────
rasterio>=1.3.10            # BSD
pyproj>=3.6.0               # MIT

# ── ASR — runs fully locally, no API key needed ─────
faster-whisper>=1.0.3       # MIT  — CTranslate2 Whisper (CPU/GPU)
openai-whisper>=20240930    # MIT  — reference / fine-tune
whisper-timestamped>=1.15.0 # MIT  — forced alignment
transformers>=4.44.0        # Apache-2.0
timm>=0.9.0                 # Apache-2.0 — required by Florence-2 DaViT vision encoder
datasets>=2.21.0            # Apache-2.0
peft>=0.12.0                # Apache-2.0 — LoRA fine-tune
nltk>=3.9.0                 # Apache-2.0 — BLEU evaluation

# ── Speaker diarization ─────────────────────────────
speechbrain>=1.0.0          # Apache-2.0 — X-vector embeddings
pyannote.audio>=3.3.1       # MIT        — diarization pipeline
# nemo_toolkit[asr]         # Apache-2.0 — MSDD (optional, large install)

# ── Visual Speech Recognition ───────────────────────
mediapipe>=0.10.14          # Apache-2.0 — mouth ROI (VSR-IMPL-02)
av>=13.0.0                  # BSD        — PyAV video decode
pytorchvideo>=0.1.5         # Apache-2.0 — temporal clip sampling
# fairseq                   # MIT        — AV-HuBERT backbone (install from GitHub)

# ── Evaluation metrics ──────────────────────────────
rouge-score>=0.1.2          # Apache-2.0 — ROUGE-1/2/L for ASR summary eval

# ── Vector DB ───────────────────────────────────────
qdrant-client>=1.11.0       # Apache-2.0 — X-vector + frame cache store

# ── Workflow orchestration ───────────────────────────
crewai>=0.130.0             # MIT — multi-agent workflow runtime

# ── REST API ────────────────────────────────────────
fastapi>=0.115.0            # MIT
uvicorn[standard]>=0.30.0   # BSD
python-multipart>=0.0.12    # Apache-2.0
pydantic>=2.9.0             # MIT
streamlit>=1.37.0           # Apache-2.0 — browser UI

# ── Observability ───────────────────────────────────
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp>=1.27.0

# ── Cloud storage (optional) ────────────────────────
boto3>=1.35.0               # Apache-2.0

# ── Claude API (optional cloud LLM backend) ─────────
# Only needed when --llm claude is used.
# Ollama (--llm ollama) requires no extra Python package.
anthropic>=0.36.0           # MIT

# ── Sentence embeddings (semantic frame cache) ───────
sentence-transformers>=3.1.0  # Apache-2.0

# ── Dev / Test ──────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
ruff>=0.6.0
mypy>=1.11.0
pre-commit>=3.8.0
